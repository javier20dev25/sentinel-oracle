import React from 'react';
interface StatusBarProps {
    provider: string;
    isConnected: boolean;
}
export declare function StatusBar({ provider, isConnected }: StatusBarProps): React.JSX.Element;
export {};
